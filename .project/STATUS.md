# Tract delivery status

## Current stage

Milestones 1–9 infrastructure and domain foundations remain implemented, but product-owner acceptance is not granted and Milestone 10 is reopened. The 24 August 2026 browser review demonstrated an unscrollable roughly 10,699 px Overview dialog, the same unbounded-overlay risk elsewhere, fixed and proportionally normalized Overview accounting anchors, mixed OEM/global scope, and deterministic route-local or disabled workflows. The approved A–G correction requires one canonical scoped aggregation layer, accessible bounded overlays, an atomic recovery-activation journey, the fixed release-scope DCR workflow, materiality controls, governed automotive master data, actionable Contracts/Recoveries/Reports, unified Operations, and honest Settings. Published commit `346c23e` is the current **INCREMENT COMPLETE** baseline with approved work remaining; it is not a release candidate, production-ready, or finally accepted. The confirmed non-production Supabase staging project `qflwjgmrspmcyghzinwz` is synchronized through additive migration `202608240003`; no production, paid, or live provider has been activated, and GitHub Actions remain manual-only because credits are exhausted.

## Reopened Milestone 10 defects and authorized work

- Replace fixed `462M`/`18M`/`3M` anchors, proportional scaling, and independent route calculations with direct canonical aggregates and exact reconciliation tests.
- Make Program/Part scope visible and coherent across every KPI/chart/table/risk/detail/export, with URL state and provenance.
- Correct the shared Dialog/Sheet/Drawer boundary so long content stays within the viewport and scrolls internally with Radix focus/Escape behavior; add the demonstrated Overview and Part regressions plus 200-program/17,000-part browser evidence.
- Deliver the single atomic **Set up / activate recovery** commercial workflow while retaining separate canonical Program, Part/Revision, DCR, and Agreement records. Agreement/setup gates apply at canonical Active and posting, not at DCR draft.
- Complete the fixed-lifecycle DCR Table/Board interaction, comments/private attachments/assignments/reviewers/activity, materiality rules, automotive alias/proposal provenance, contract/recovery/report actions, the unified Operations lifecycle, and accurate Settings activation boundaries. Arbitrary property/pipeline/automation designers are deferred with no placeholder UI.
- Preserve the approved Overview hierarchy: six organization-wide derived tiles, concise organization-wide risks, quarterly/OEM row, then a clearly separate OEM→program/model→model-year→part analytical scope for the chart/table and Active Programs.
- Re-run the complete documented risk-based matrix and leave a new localhost demonstration for Gemma's review. Earlier browser evidence does not satisfy the reopened criteria.

## Reopened correction progress

- Added the canonical part-derived analysis snapshot and CSV export contract with exact model-year allocation, part→program→OEM roll-ups, gross under/over exposure, additive remaining recovery, supported forecast-version validation, materiality-rule alerts, URL scope, provenance, and reconciliation assertions.
- Rebuilt Overview in the approved order: six direct organization-wide tiles, prioritized organization-wide alerts, derived quarterly review/OEM views, then a clearly labelled OEM→program/model→model-year→part analytical section with Program/Part mode, chart/table, scoped export, and clickable bounded Active Programs.
- Replaced the shared Dialog/AlertDialog/Sheet/Drawer overflow boundary with a viewport-bounded outer shell, dedicated internal scroll region, and persistent close control. Desktop 1440×900 and mobile 390×844 checks prove the 200-program Overview breakdown remains within the viewport while its 10,775–10,910 px scroll content is reachable; Escape closes and focus returns to the originating tile.
- Extended the same canonical snapshot to Programs, Part Numbers, Forecasts, Recoveries, Reports, and their current-filter CSV/print outputs. The workspaces now share the OEM→program/model→model-year→part hierarchy, URL state, chart/table values, provenance, and evidence links instead of route-local financial calculations.
- Reworked Programs and Part Numbers into progressive analytical workspaces; ordinary-user orphan-producing creation actions now route to recovery setup, admin master-data maintenance is de-emphasized, `Platform` is labelled `Vehicle architecture (optional)`, and decorative vehicle imagery is removed.
- Added neutral, consistent under/over-recovery review queues with evidence and real scoped CSV packages, and added six filter→preview→download report families. CSV and print/save-PDF paths are functional for deterministic synthetic data; XLSX and the remaining workflow-specific report coverage stay open.
- Applied additive migration `202608240003_milestone10_acceptance_controls.sql` after an exact CLI `2.111.0` dry run. It adds audited materiality rules, governed vehicle make/architecture/provenance and master-data proposal/alias/merge seams, persisted connector-test evidence, and an administrator-only transactional agreement activation function that fails rather than partially activating invalid accruals. DCR creation remains agreement-independent; DCR Active requires an approved effective agreement and linked active recovery.
- The complete local gate passes formatting, zero-warning lint, TypeScript, 76 tests across 18 files, and the client plus SSR/Worker production build. Clean desktop and 390 px browser passes cover Programs, Part Numbers, Forecasts, Recoveries, and Reports with no document overflow or console warnings/errors; Part detail is viewport-bounded, internally scrollable, closes with Escape, and restores focus.

## Completed

- Preserved the supplied artifacts and original extracted prototype under `.project/prototype`, then promoted the working application to the repository root.
- Standardized the Node/pnpm toolchain, lockfile, lint/format/type/test/build gates, GitHub Actions workflow, environment validation, health endpoint, structured request logging, and Cloudflare Worker build.
- Added organization-bound schema, composite tenant constraints, memberships/additive grants, validated RLS helpers and policies, audit/outbox infrastructure, and cross-tenant denial tests.
- Added DCR master data, configurable workflow and permissions, immutable history, assignments/comments, private-attachment model, and a functional labelled demonstration workflow.
- Added the exact-decimal recovery ledger with versioned policies, effective rates, immutable signed volume, correction/duplicate/over-recovery handling, reproducible results, and tests.
- Added CSV/Excel staging, declarative mappings, SHA-256 idempotency, generic HTTPS REST and SAP extension contracts, reconciliation schema, and a functional local operations screen.
- Added the generic server-side REST runtime with runtime-only credential resolution, exact host allowlisting, redirect denial, abort timeout, bounded transient retries/backoff, JSON/byte/record limits, and secret-free results.
- Confirmed and implemented the three ingestion domains: replaceable IHS/AFS vehicle volume, evidence-backed contract/DCR documents, and customer-mapped SAP/ERP operational data.
- Added immutable original/raw records, versioned canonical candidates, a permissioned Received→Staged→Validated→Mapped→Reviewed→Approved→Posted lifecycle, exception/reconciliation records, and a unique economic-event posting registry.
- Added OEM/program/model/plant/region/part mappings; effective parts-per-vehicle, take-rate, allocation, and eligible-volume policies; immutable vehicle-production and ERP records; and ledger references that prevent external production totals from becoming part volume without approved contract rules.
- Added distinct Program and Part/Revision creation workflows, canonical recovery agreements with effective terms and record links, a dedicated Contracts workspace, and additive staging integrity guards for agreement-gated calculations, immutable approved revisions, exact connector host allowlists, and declarative mapping keys.
- Rebuilt DCRs as an evidence-gated horizontal pipeline with search, status/history filters, accountable assignments, comments, immutable transition evidence, and direct Program/Part/Agreement links. A DCR can be drafted without an agreement; recovery activation and posting remain agreement-gated.
- Added the plain-language Data Connections workspace and guided file/API/SAP-extension wizard with tenant, environment, transport, endpoint/host boundary, authentication method, opaque secret reference, schedule/delta behavior, source objects/categories, versioned no-code mappings, reconciliation, retry, owner, health, lifecycle, and audit views. Safe synthetic/configuration validation works; live provider tests remain explicitly blocked without approved specifications and runtime credentials.
- Added hierarchical selection, sorting/filtering/pagination, bounded 25-program and 50-part rendering, direct forecast/contract links, calculation/provenance drill-downs, real synthetic part-to-commodity relationships, functional Profile/Organization routes, and explicit reasons on unavailable actions. Inaccurate vehicle imagery and the unsupported API Keys control were removed.
- Added document text/table/field candidates with page/table evidence, confidence/warnings, correction reasons, reviewer identity, immutable approval, and controlled destination postings.
- Added versioned forecast provenance, secure document lifecycle/provider/review interfaces, deterministic local extraction, safe reports/evidence manifests, approvals, and notification outbox.
- Added clean-scan/document-hash gating, required evidence and correction reasons, a complete synthetic manual-review/posting-plan path, canonical recovery replay hashes, explicit partial-import/retry/cancel/reconciliation controls, and destination-at-delivery notification adapters.
- Routed the built Worker through the hardened custom server entry with CSP/isolation/frame/referrer/permissions/cache controls, validated request ids, structured request events, and catastrophic SSR normalization.
- Pinned the database CLI in CI, added the production dependency audit, weekly dependency/Actions updates, CI concurrency, static migration contract tests, and a build-entry regression test.
- Removed external runtime assets and unsupported affirmative claims, repaired responsive navigation/overflow, and documented architecture, accounting, security, imports, operations, deployment, costs, assumptions, milestones, and release controls.
- Verified current official Supabase and Cloudflare pricing; the estimated low-usage pilot base is about US$30/month before excluded services. Nothing paid has been activated.

## Verification

- The current working increment passes formatting, zero-warning lint, TypeScript, 76 tests across 18 files, and the client plus SSR/Worker production build. The build's expected fail-closed warning names the absent Supabase bindings; no credential was supplied or exposed.
- Tests cover recovery precision/boundaries/canonical replay, authorization, DCR transitions, imports/Excel/partial approval/retry/cancellation, configuration, ingestion reconciliation, connector runtime security, scanned document intake/review/posting, notification delivery, response hardening, migration invariants, and runtime configuration.
- Production dependency audit: no known vulnerabilities.
- Browser smoke: all 13 routes render with the demonstration boundary visible and no console warnings/errors.
- Functional browser checks passed for Overview/Parts sorting, filtering, pagination, and drill-downs; DCR evidence denial and approval history; agreement original/evidence/link/rule review; connector category/mapping/reconciliation validation and live-test denial; CSV valid/rejected staging; synthetic document review; Forecast deep links; and Profile/Organization routes.
- Updated Operations browser check: all three confirmed ingestion domains, shared lifecycle, vehicle-volume staging, valid/rejected row traceability, synthetic document field/evidence/reason review, and a three-action controlled posting plan passed with no console warnings/errors.
- Responsive browser check at 390 x 844: all 13 routes have no document-level horizontal overflow or duplicate ids; the primary navigation is available in the labelled `Tract navigation` dialog.
- Accessibility sampling verified named critical form controls, explicit disabled-control reasons, focusable named part drill-down controls, semantic tables, image alternatives, and the mobile navigation dialog. Full manual assistive-technology and supported-browser certification remains external.
- Live local health response verified fail-closed `503` behavior plus no-store, CSP, frame denial, opener isolation, and malformed request-id replacement. Bundle inspection verified the hardened entry is present in `dist/server/index.js`.
- Static migration tests pass for order, transaction wrapping, RLS coverage, security-definer `search_path`, critical immutability/posting guards, and credential-shaped text. Cloudflare Worker production build passes without connecting an account.
- Static migration tests also enforce the private document bucket, size limit, absence of direct authenticated object policies, and server-signed access contract.
- The named `tract-recovery-platform-staging` Cloudflare environment declares every required Supabase binding without values, retains structured logging, and passes a real no-upload Wrangler bundle dry run at 2.86 MiB uncompressed and 604 KiB gzip.
- Repository scan found no credential/token patterns, authorship references, or newly tracked customer artifacts. Three pre-existing root binary artifacts remain outside Git and were not opened, changed, or staged.
- GitHub Actions successfully created a fresh Supabase stack, applied all three migrations, passed database lint, and passed both pgTAP suites. Its first RLS run exposed an incorrect test expectation: PostgreSQL denied a member's role update by matching zero rows rather than raising an exception. The corrected regression verifies that the role remains unchanged, and both hosted CI jobs pass at commit `27929e4`.
- Authenticated staging preflight on 24 August 2026 verified the exact project URL, an empty application migration ledger/schema, zero Auth users and Storage objects, no application policies, and no pre-migration security or performance advisor findings. The local migration SHA-256 values still match the verified handoff, and the six static migration contract tests pass.
- Supabase CLI `2.111.0` initially dry-ran and applied only unchanged repository migrations `202608210001_foundation.sql`, `202608210002_product.sql`, and `202608210003_ingestion_domains.sql`. Milestone 10 migrations `202608240001`–`003` were inspected, separately dry-run, and applied through the CLI; current CLI/MCP inspection reports exactly those six ledger versions and names in order. The first `003` push attempt failed transactionally because the existing `vehicle_models` table had to be extended rather than recreated; the ledger remained unchanged, the migration was corrected additively, and the successful push followed a fresh exact dry run. No MCP migration application or ledger repair was used.
- Linked staging contains 70 public tables with RLS enabled, 132 public policies, 38 application functions, 149 non-internal public triggers, the required immutability/transition/posting/integrity guards, and the private `tract-private-documents` bucket with a 25 MiB limit and no direct authenticated object policy. Auth users and Storage objects remain empty after validation.
- Linked database lint passes at warning-as-failure. Repository SQL assertions pass transactionally for RLS (7), ingestion (10), private Storage RLS (2), Milestone 10 workflow/isolation including atomic activation (16), integrity guards (3), and acceptance controls (22): 60 assertions total, of which 58 are database pgTAP and 2 are Storage-policy assertions. Docker is unavailable locally, so the database suites were executed against linked staging by composing the repository synthetic seed into each rolled-back test transaction; no seed records were retained.
- Supabase Auth and Storage service health pass using runtime-only credentials. Direct authenticated Storage insertion is denied and object listing exposes no private rows. Hosted Auth currently reports self-signup enabled even though the repository configuration disables it, so invitation/reset/MFA/session and multi-organization browser flows are not accepted as complete.
- Post-migration security advisors are clean. Performance advisors report 288 unindexed-foreign-key notices, 17 unused-index notices on the empty database, 4 Auth RLS initialization-plan warnings, and 49 multiple-permissive-policy warnings. These 358 non-security findings are recorded for workload-informed hardening; they do not authorize speculative index removal or a bulk policy rewrite.
- Project database types were generated from linked staging and wired into both user-scoped and service-role Supabase clients.

## Genuine activation blockers

- The remaining credential-free Milestone 10 correction is not externally blocked. Contracts recovery setup, the release-scope DCR Table/Board experience, unified Operations lifecycle, Settings actions, and the complete revised browser matrix still require implementation before product-owner review resumes.

- Hosted Auth configuration requires approved site/redirect URLs, invitation and password-reset behavior, MFA/session policy, multi-organization rules, and a controlled configuration change that disables the currently enabled public signup path. Browser acceptance needs invited test identities supplied through the approved credential path.
- End-to-end private document upload, malware scan, short-lived signed read, expiry, and replacement require an approved scanning/extraction provider, sanitized documents, credentials, retention rules, and paid-service approval where applicable. The database and Storage denial boundary is verified, but unsupported provider behavior remains disabled.
- Performance advisor findings require workload-informed index selection and an explicit review of intentional additive RLS policies before a separately traceable migration; empty-database unused-index notices are not sufficient evidence for removal.
- A Cloudflare account/project and controlled secret values are required for a staging deployment and hosted log/health verification.
- Production IHS/AFS, SAP/ERP, document extraction/scanning, notification, and SSO providers require approved specifications, credentials, licensing/data-processing terms, and any paid-service approval.
- Authoritative customer rules remain required for FX, settlement rounding, claim eligibility, clawbacks/profit release, retention, residency, RPO/RTO, notification recipients, and formal compliance commitments. Existing behavior remains versioned/configurable or disabled.
- The GitHub remote and non-production Supabase staging database are active. No production resource, Cloudflare deployment, external provider connection, or paid service has been activated. The complete input checklist is [docs/external-activation.md](../docs/external-activation.md).

## Exact next step

Commit and push the verified canonical-analytics and additive activation-control increment after exact `pnpm check`. Then implement the Contracts atomic guided setup and the fixed-lifecycle DCR Table/Board workflow, followed by unified Operations/Data Connections and Settings. Each coherent increment requires targeted tests and exact `pnpm check` before push. Any later schema change must be additive after `202608240003`, dry-run before staging apply, and followed by exact ledger/RLS/pgTAP/lint/advisor/type verification. Product-owner review resumes only after the complete revised localhost risk-based matrix passes.
